<?xml version="1.0" encoding="UTF-8" ?>

<testproblem>
  <name>test gmsh on intel</name>
  <owner userid="jhill1"/>
  <tags>flml intel</tags>
  <problem_definition length="short" nprocs="1">
      <command_line>ldd fluidity > ldd_output; fluidity -v1 -l boring.flml</command_line>
    <!-- Trigger error messgae on intel compiler. should produce output on other systems. -->
  </problem_definition>
  <variables>
    <variable name="solvers_converged" language="python">
import os
files = os.listdir("./")
solvers_converged = not "matrixdump" in files and not "matrixdump.info" in files
    </variable>
</variables>
   <pass_tests>
    <test name="Solvers converged" language="python">
      assert(solvers_converged)
    </test>
      <test name="Correct error message" language="python">
f = open("ldd_output","r")
output = f.read()
if (output.find('intel') > -1):
    intel = True
else:
    intel = False
f.close()
f = open("fluidity.err-0","r")
output = f.read()
if (intel):
    assert(output.find("There are issues with the Intel compiler and GMSH ASCII files") > -1)
else:
    assert(len(output) == 0)
      </test>
  </pass_tests>
  <warn_tests>
  </warn_tests>
</testproblem>
