<?xml version="1.0" encoding="UTF-8" ?>
<testproblem>
  <name>lagrangian_detectors_increasing_rotation</name>
  <owner userid="drhodrid"/>
  <tags>flml parallel zoltan</tags>
  <problem_definition length="short" nprocs="4">
    <command_line>
mpiexec ../../bin/flredecomp -i 1 -o 4 -v -l lagrangian-detectors-increasing-rotation lagrangian-detectors-increasing-rotation-flredecomp;
mpiexec ../../bin/fluidity -v3 -l lagrangian-detectors-increasing-rotation-flredecomp.flml
    </command_line>
  </problem_definition>
  <variables>
    <variable name="last_single_locations_error" language="python">
from H5hutpy import *
import numpy as np
last_single_locations_error = np.zeros((2,3))
X=np.zeros(100,float)
Y=np.zeros(100,float)
f = open('Xvals.npy', "r")
X_in= np.fromfile(f,dtype=np.float)
f = open('Yvals.npy', "r")
Y_in= np.fromfile(f,dtype=np.float)
for i in range(0,100):
  X[i]=X_in[i+10]
  Y[i]=Y_in[i+10]

f = H5OpenFile("lagrangian_detectors.particles.Singl_Lagr_1.h5part", H5_O_RDONLY, H5_PROP_DEFAULT)
nsteps = H5GetNumSteps(f)
H5SetStep(f, nsteps-1)
pos = np.empty((2,1), dtype='float64')
H5PartReadDataFloat64(f, 'x', pos[0,:])
H5PartReadDataFloat64(f, 'y', pos[1,:])
last_single_locations_error[0,0] = pos[0,:] - X[0]
last_single_locations_error[1,0] = pos[1,:] - Y[0]

f = H5OpenFile("lagrangian_detectors.particles.Singl_Lagr_2.h5part", H5_O_RDONLY, H5_PROP_DEFAULT)
nsteps = H5GetNumSteps(f)
H5SetStep(f, nsteps-1)
pos = np.empty((2,1), dtype='float64')
H5PartReadDataFloat64(f, 'x', pos[0,:])
H5PartReadDataFloat64(f, 'y', pos[1,:])
last_single_locations_error[0,1] = pos[0,:] - X[50]
last_single_locations_error[1,1] = pos[1,:] - Y[50]

f = H5OpenFile("lagrangian_detectors.particles.Singl_Lagr_3.h5part", H5_O_RDONLY, H5_PROP_DEFAULT)
nsteps = H5GetNumSteps(f)
H5SetStep(f, nsteps-1)
pos = np.empty((2,1), dtype='float64')
H5PartReadDataFloat64(f, 'x', pos[0,:])
H5PartReadDataFloat64(f, 'y', pos[1,:])
last_single_locations_error[0,2] = pos[0,:] - X[99]
last_single_locations_error[1,2] = pos[1,:] - Y[99]
    </variable>
    <variable name="last_locations_error" language="python">
from H5hutpy import *
import numpy as np
last_locations_error = np.zeros((2,100))
X=np.zeros(100,float)
Y=np.zeros(100,float)
f = open('Xvals.npy', "r")
X_in= np.fromfile(f,dtype=np.float)
f = open('Yvals.npy', "r")
Y_in= np.fromfile(f,dtype=np.float)
for i in range(0,100):
  X[i]=X_in[i+10]
  Y[i]=Y_in[i+10]

f = H5OpenFile("lagrangian_detectors.particles.Steve.h5part", H5_O_RDONLY, H5_PROP_DEFAULT)
nsteps = H5GetNumSteps(f)
H5SetStep(f, nsteps-1)
ids = np.empty(100, dtype='int32')
H5PartReadDataInt32(f, 'id', ids)
pos = np.empty((2,100), dtype='float64')
H5PartReadDataFloat64(f, 'x', pos[0,:])
H5PartReadDataFloat64(f, 'y', pos[1,:])
idx = np.argsort(ids)
pos = pos[:,idx]
print(pos[0,:],pos[1,:])
last_locations_error[0,:] = pos[0,:] - X[:]
last_locations_error[1,:] = pos[1,:] - Y[:]
    </variable>
  </variables>
  <pass_tests>
    <test name="CorrectSingleLagrangianDetectors" language="python">
      assert abs(last_single_locations_error).max() &lt; 1.0e-6
    </test>
    <test name="CorrectLagrangianTrajectory" language="python">
      assert abs(last_locations_error).max() &lt; 1.0e-6
    </test>
  </pass_tests>
  <warn_tests>
  </warn_tests>
</testproblem>
